import { describe, expect, it } from 'vitest'
import useFetchShows from '~api/useFetchShows'

describe('useFetchShows api', () => {
  it('is defined', () => {
    expect(useFetchShows).toBeDefined()
  })
})
