import { describe, expect, it } from 'vitest'
import useFetchOneShow from '~api/useFetchOneShow'

describe('useFetchOneShow api', () => {
  it('is defined', () => {
    expect(useFetchOneShow).toBeDefined()
  })
})
