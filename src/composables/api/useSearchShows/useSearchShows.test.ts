import { describe, expect, it } from 'vitest'
import useSearchShows from '~api/useSearchShows'

describe('useSearchShows api', () => {
  it('is defined', () => {
    expect(useSearchShows).toBeDefined()
  })
})
